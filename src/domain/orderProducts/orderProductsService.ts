// import OrderProductsModel from './orderProductsModel';
import { OrderProduct } from './orderProductEntity';

const OrderProductsService = {
  async createItemInOrderId() {
    // const orderExists = await OrdersModel.count({
    //   where: {
    //     id: orderId,
    //   },
    // });
    // if (!orderExists) throw new Error('Order not found');
    // const orderDetails = await OrdersModel.findAll({
    //   where: {
    //     id: orderId,
    //   },
    //   include: ['order_products', 'products'],
    // });
    // return orderDetails;
  },
  async getItemsInOrderId(orderId: string) {
    // const orderExists = await OrdersModel.count({
    //   where: {
    //     id: orderId,
    //   },
    // });
    // if (!orderExists) throw new Error('Order not found');
    // const orderDetails = await OrdersModel.findAll({
    //   where: {
    //     id: orderId,
    //   },
    //   include: ['order_products', 'products'],
    // });
    // return orderDetails;
  },
  async updateItemInOrderId(orderId: string) {
    // const orderExists = await OrdersModel.count({
    //   where: {
    //     id: orderId,
    //   },
    // });
    // if (!orderExists) throw new Error('Order not found');
    // const orderDetails = await OrdersModel.findAll({
    //   where: {
    //     id: orderId,
    //   },
    //   include: ['order_products', 'products'],
    // });
    // return orderDetails;
  },
  async deleteItemInOrderId(orderId: string) {
    // const orderExists = await OrdersModel.count({
    //   where: {
    //     id: orderId,
    //   },
    // });
    // if (!orderExists) throw new Error('Order not found');
    // const orderDetails = await OrdersModel.findAll({
    //   where: {
    //     id: orderId,
    //   },
    //   include: ['order_products', 'products'],
    // });
    // return orderDetails;
  },
};

export default OrderProductsService;
